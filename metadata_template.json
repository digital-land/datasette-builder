{
  "title": "S3 Parquet Files",
  "description": "Datasette instance for querying Parquet files from an S3 bucket",
  "settings": {  
    "suggest_facets": false
  },
  "plugins": {
    "digital-land-datasette": {
      "issue": {
        "bucket": "$COLLECTION_DATA_BUCKET",
        "endpoint_url": "$COLLECTION_DATA_URL",
        "directory": "s3://$COLLECTION_DATA_BUCKET/log/issue",
        "httpfs": true
      },
      "expectation": {
        "bucket": "$COLLECTION_DATA_BUCKET",
        "endpoint_url": "$COLLECTION_DATA_URL",
        "directory": "s3://$COLLECTION_DATA_BUCKET/log/expectation",
        "httpfs": true
      }
    }
  }
}

