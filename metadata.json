{
    "databases": {
        "development-plan-document": {
            "tables": {
                "slug": {
                    "label_column": "slug"
                },
                "entry": {
                    "label_column": "entity"
                },
                "fact": {
                    "label_column": "attribute"
                }
            }
        },
        "fixtures": {
            "queries": {
                "point_geography": {
                    "sql": "SELECT * FROM geography WHERE geometry IS NOT NULL AND WITHIN(GeomFromText('POINT(' || :latitude || ' ' || :longitude)'), geom)",
                    "title": "Geographies that encompass a point"
                }
            }
        }
    }
}
