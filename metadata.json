{
    "plugins": {
        "datasette-cors": {
            "host_wildcards": ["*"]
        }
    },
    "databases": {
        "view_model": {
            "tables": {
                "slug": {
                    "label_column": "slug"
                }
            },
            "queries": {
                "point_geography": {
                    "sql": "SELECT geography, name, geometry FROM geography WHERE geometry IS NOT NULL AND WITHIN(GeomFromText('POINT(' || :longitude || ' ' || :latitude || ')'), geom)",
                    "title": "Geographies that encompass a point"
                },
                "bounded_geography": {
                    "sql": "SELECT * FROM geography WHERE geography.rowid in (SELECT pkid FROM idx_geography_geom WHERE xmin > :bbox_minx AND xmax < :bbox_maxx AND ymin > :bbox_miny AND ymax < :bbox_maxy)",
                    "title": "Geographies inside bounding box"
                }
            }
        },
        "development-plan-document": {
            "tables": {
                "entry": {
                    "label_column": "entity"
                },
                "fact": {
                    "label_column": "attribute"
                }
            }
        }
    }
}
